// Temporary compatibility copy of the v1.3 core. Loaded through app.js bootloader.
// This file intentionally remains separate so stale iPhone/PWA index pages that did
// not reference solver.js can still recover by letting app.js load dependencies first.
